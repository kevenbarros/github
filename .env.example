# Exemplo de arquivo .env para configuração da API do GitHub
# Renomeie este arquivo para .env ou .env.local

# URL base da API (geralmente não precisa mudar)
VITE_API_BASE_URL=https://api.github.com

# Timeout das requisições em milissegundos
VITE_API_TIMEOUT=15000

# Token do GitHub (opcional, mas recomendado para evitar rate limits)
# Gere um token em: https://github.com/settings/tokens
# VITE_GITHUB_TOKEN=ghp_seu_token_aqui

# Usuário padrão
VITE_DEFAULT_USER=octocat